/**
 * EmailJS is designed to be called from the browser with these identifiers
 * exposed client-side — the public key is rate-limited and domain-locked on
 * EmailJS's side, so none of this needs to be a secret.
 */
export const emailjsConfig = {
  serviceId: "service_p6nna1t",
  templateId: "template_5pl1zth",
  publicKey: "aMoTOelAqR288P_Cr",
} as const;
